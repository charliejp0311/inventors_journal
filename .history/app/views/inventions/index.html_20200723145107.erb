<h1><%=@user.name%>'s Invention's</h1>
<ul>
<%= form_for :invention do |f| %>
    <%= f.label :title %>
    <%= f.text_field :title %>
<% end %>

<% @user.inventions.each do |invent|%>
    <li><%= link_to invent.title, invention_path(invent)%></li>
<%end%>
</ul>