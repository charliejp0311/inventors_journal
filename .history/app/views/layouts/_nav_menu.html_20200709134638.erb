<div class="topnav">
<% if logged_in? %>
    <i><%= link_to "#{current_user.name} ~ Inventions", user_inventions_path(current_user)%></i><span><%=link_to 'Sign Out', '/signout'%>
<% else %>
    <%= link_to "New User", new_user_path %>
    <%= link_to "Existing User", '/signin' %>
<% end %>
</div>